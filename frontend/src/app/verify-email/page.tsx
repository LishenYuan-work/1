"use client";
import Link from "next/link"; import { useEffect, useState } from "react"; import { api } from "@/lib/api";
export default function VerifyEmailPage(){const [message,setMessage]=useState("正在验证邮箱...");useEffect(()=>{Promise.resolve().then(()=>{const token=new URLSearchParams(window.location.search).get("token");if(!token){setMessage("缺少验证令牌");return}api.auth.verify(token).then(()=>setMessage("邮箱验证成功，请登录工作台。"),err=>setMessage(err instanceof Error?err.message:"验证失败"))})},[]);return <main className="auth-shell"><section className="auth-card"><div className="brand"><span className="brand-mark">R</span> 多 Agent 交叉评审</div><h1>邮箱验证</h1><p>{message}</p><Link className="primary" style={{display:"inline-block",textDecoration:"none"}} href="/login">返回登录</Link></section></main>}
