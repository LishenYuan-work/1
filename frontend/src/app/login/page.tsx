"use client";
import Link from "next/link"; import { FormEvent, useState } from "react"; import { useRouter } from "next/navigation"; import { api } from "@/lib/api"; import { useAuth } from "@/lib/auth";
export default function LoginPage() { const { login } = useAuth(); const router = useRouter(); const [email,setEmail]=useState(""); const [password,setPassword]=useState(""); const [error,setError]=useState(""); const [message,setMessage]=useState(""); const [busy,setBusy]=useState(false); async function submit(e:FormEvent){e.preventDefault();setBusy(true);setError("");setMessage("");try{await login(email,password);router.push("/")}catch(err){setError(err instanceof Error?err.message:"登录失败")}finally{setBusy(false)}} async function resend(){try{await api.auth.resend(email);setError("");setMessage("验证邮件已重新发送；本地开发请查看后端终端中的验证链接。")}catch(err){setError(err instanceof Error?err.message:"发送失败")}} return <main className="auth-shell"><section className="auth-card"><div className="brand"><span className="brand-mark">R</span> 多 Agent 交叉评审</div><h1>登录工作台</h1><p>使用已验证的邮箱进入组织评审空间。</p>{error&&<div className="error">{error}</div>}{message&&<div className="error" style={{color:"var(--accent)",background:"var(--accent-soft)",borderColor:"#cce3d3"}}>{message}</div>}<form className="auth-form" onSubmit={submit}><div className="field"><label htmlFor="email">邮箱</label><input className="input" id="email" type="email" required value={email} onChange={e=>setEmail(e.target.value)} /></div><div className="field"><label htmlFor="password">密码</label><input className="input" id="password" type="password" required value={password} onChange={e=>setPassword(e.target.value)} /></div><button className="primary" disabled={busy}>{busy?"登录中":"登录"}</button></form>{error.includes("尚未验证")&&<button className="secondary" style={{marginTop:12,width:"100%"}} onClick={resend} disabled={!email||busy}>重新发送验证邮件</button>}<p style={{marginTop:18,fontSize:12}}><Link className="auth-link" href="/register">创建组织工作区</Link> · <Link className="auth-link" href="/forgot-password">忘记密码</Link></p></section></main> }
